% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llama.R
\name{llama_state_seq_file}
\alias{llama_state_seq_file}
\alias{llama_state_seq_save_file}
\alias{llama_state_seq_load_file}
\title{Save and load the state of a single sequence to a file}
\usage{
llama_state_seq_save_file(ctx, path, seq_id, tokens = NULL)

llama_state_seq_load_file(ctx, path, seq_id, n_token_capacity = 65536L)
}
\arguments{
\item{ctx}{Context handle returned by [llama_new_context]}

\item{path}{Path to the session file.}

\item{seq_id}{Sequence ID (0-based).}

\item{tokens}{Integer vector of the tokens that produced this state, or
`NULL` to store none.}

\item{n_token_capacity}{Maximum number of tokens to read back. Must be at
least as large as the count that was saved.}
}
\value{
`llama_state_seq_save_file()`: bytes written, invisibly.
  `llama_state_seq_load_file()`: a list with `n_bytes` and the `tokens` that
  were stored with the state.
}
\description{
The file-backed counterpart of [llama_state_seq_get_data]. The token list
that produced the state is stored alongside it, so a reloaded sequence can
report which prompt it came from.
}
\examples{
\dontrun{
toks <- llama_tokenize(ctx, "A long shared prefix")
llama_state_seq_save_file(ctx, "prefix.bin", seq_id = 0L, tokens = toks)

ctx2 <- llama_new_context(model)
res  <- llama_state_seq_load_file(ctx2, "prefix.bin", seq_id = 0L)
identical(res$tokens, toks)
}
}
\seealso{
[llama_state_seq], [llama_state_save]
}
