% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llama.R
\name{llama_state_seq}
\alias{llama_state_seq}
\alias{llama_state_seq_get_size}
\alias{llama_state_seq_get_data}
\alias{llama_state_seq_set_data}
\title{Save and restore the state of a single sequence}
\usage{
llama_state_seq_get_size(ctx, seq_id, partial_only = FALSE, on_device = FALSE)

llama_state_seq_get_data(ctx, seq_id, partial_only = FALSE, on_device = FALSE)

llama_state_seq_set_data(
  ctx,
  data,
  seq_id,
  partial_only = FALSE,
  on_device = FALSE
)
}
\arguments{
\item{ctx}{Context handle returned by [llama_new_context]}

\item{seq_id}{Sequence ID (0-based), below `llama_n_seq_max(ctx)`.}

\item{partial_only}{Copy only partial state (SWA window / recurrent state).}

\item{on_device}{Keep the copy in device memory where the backend supports it.}

\item{data}{A raw vector previously returned by `llama_state_seq_get_data()`.}
}
\value{
`llama_state_seq_get_size()`: size in bytes.
  `llama_state_seq_get_data()`: a raw vector.
  `llama_state_seq_set_data()`: bytes read, invisibly.
}
\description{
Where [llama_state_get_data] snapshots the entire context, these work on one
sequence at a time. That makes it possible to checkpoint a single
conversation in a multi-sequence context, or to cache the KV state of a long
shared prefix and restore it into a fresh sequence instead of recomputing it.
}
\details{
`partial_only = TRUE` restricts the copy to partial state — the SWA window of
a sliding-window model, or the recurrent state of a Mamba-style model. Leave
it `FALSE` for ordinary transformers.
}
\examples{
\dontrun{
ctx <- llama_new_context(model, n_seq_max = 4L)

# Cache the KV state of a shared prefix, then reuse it for another sequence
prefix <- llama_state_seq_get_data(ctx, seq_id = 0L)
llama_state_seq_set_data(ctx, prefix, seq_id = 1L)
}
}
\seealso{
[llama_state_seq_save_file], [llama_state_data], [llama_memory_seq_cp]
}
