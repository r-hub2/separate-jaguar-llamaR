% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llama.R
\name{llama_sampler_chain_from_params}
\alias{llama_sampler_chain_from_params}
\title{Build a sampler chain from a parameter list}
\usage{
llama_sampler_chain_from_params(
  ctx,
  params,
  grammar = NULL,
  trigger_patterns = NULL,
  trigger_tokens = NULL
)
}
\arguments{
\item{ctx}{Context handle returned by [llama_new_context]. The chain reads
the model's vocabulary, so it is tied to this context's model.}

\item{params}{A parameter list from [llama_sampler_params].}

\item{grammar}{GBNF grammar string, or \code{NULL}.}

\item{trigger_patterns, trigger_tokens}{Lazy-grammar triggers; see
[llama_generate].}
}
\value{
An external pointer of class \code{"llama_sampler_chain"}.
}
\description{
Assembles the same chain the generation functions build internally from a
[llama_sampler_params] list, but hands it back for inspection or adjustment.
Use it to see which samplers a given parameter list actually produces, and in
what order.
}
\examples{
\dontrun{
sp <- llama_sampler_params(temp = 0.8, dry_multiplier = 0.8)
chain <- llama_sampler_chain_from_params(ctx, sp)
vapply(seq_len(llama_sampler_chain_n(chain)) - 1L,
       function(i) llama_sampler_name(llama_sampler_chain_get(chain, i)),
       character(1))
}
}
\seealso{
[llama_sampler_params], [llama_sampler_chain_new]
}
